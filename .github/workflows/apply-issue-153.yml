name: Apply issue 153 fix

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [master]

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.ref == 'agent/align-ctap-status-codes'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.91
          components: rustfmt

      - name: Apply source changes
        run: python3 scripts/apply_issue_153.py

      - name: Format
        run: cargo fmt --all

      - name: Test CTAP status handling
        run: cargo test -p soft-fido2-ctap

      - name: Commit final patch
        env:
          BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          rm -f scripts/apply_issue_153.py .github/workflows/apply-issue-153.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: align CTAP status codes with FIDO 2.3"
          git push origin "HEAD:${BRANCH}"
