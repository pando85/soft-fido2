name: Apply issue 136 patch

on:
  push:
    branches:
      - agent/fix-cm-rp-scope
  pull_request:
    branches:
      - master

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/fix-cm-rp-scope
      - name: Apply patch
        run: |
          if git apply --check .agent/issue-136.patch; then
            git apply .agent/issue-136.patch
          else
            echo "Patch is already applied"
            exit 0
          fi
      - name: Commit source changes
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add soft-fido2-ctap/src/pin_token.rs
          git commit -m "fix credential management RP scope [agent-applied]"
          git push origin HEAD:agent/fix-cm-rp-scope
